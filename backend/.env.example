# Server Port
PORT=3000

# Serial Port (for local Arduino connection)
# Windows: COM3, COM4, etc.
# Linux/Raspberry Pi: /dev/ttyUSB0, /dev/ttyACM0
SERIAL_PORT=COM3

# When deployed on Vercel, Arduino data comes via HTTP POST from read_arduino.py
